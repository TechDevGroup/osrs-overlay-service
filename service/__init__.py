"""osrs-overlay-service — the logic half of a thin-client RuneLite overlay.

The RuneLite plugin is a dumb renderer; this package holds all Blast Furnace
domain logic and speaks the newline-delimited JSON bridge protocol (PROTOCOL.md).
"""
__version__ = "0.1.0"
